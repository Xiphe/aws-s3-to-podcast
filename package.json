{
  "name": "replaced-by-user-input",
  "description": "replaced-by-user-input",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --outfile=build/handler.js --platform=node --target=node14 --sourcemap",
    "deploy": "sam deploy --config-env prod",
    "predeploy": "npm run build",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.91",
    "aws-sdk-mock": "^5.1.0",
    "esbuild": "0.14.13"
  }
}
